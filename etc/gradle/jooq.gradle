jooq {
    configuration {
        jdbc {
            url = "jdbc:mysql://localhost:3306/florist"
            user = "florist"
            password = "florist"
        }
        generator {
            name = 'org.jooq.codegen.DefaultGenerator'
            database {
                name = 'org.jooq.meta.mysql.MySQLDatabase'
                inputSchema = 'florist'
            }
            target {
                packageName = 'com.example.florist.generated.db'
                directory = "src/main/java/"
            }
        }
    }
}
